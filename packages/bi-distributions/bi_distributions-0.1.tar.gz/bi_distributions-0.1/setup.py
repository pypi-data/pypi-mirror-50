from setuptools import setup

setup(name='bi_distributions',
      version='0.1',
      description='Gaussian distributions',
      packages=['bi_distributions'],
      author = 'Rakesh',
      zip_safe=False)
