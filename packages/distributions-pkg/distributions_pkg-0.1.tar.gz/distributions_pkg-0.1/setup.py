from setuptools import setup

setup(name='distributions_pkg',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions_pkg'],
      zip_safe=False)
