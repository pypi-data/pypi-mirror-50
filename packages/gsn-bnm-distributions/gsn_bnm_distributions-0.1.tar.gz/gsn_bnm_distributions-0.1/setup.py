from setuptools import setup

setup(name='gsn_bnm_distributions',
      version='0.1',
      description='gaussian and binomial probability distributions',
      packages=['gsn_bnm_distributions'],
      zip_safe=False)
